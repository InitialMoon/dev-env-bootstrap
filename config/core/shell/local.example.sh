# Copy to ~/.config/my-linux-config/shell/local.sh for machine-specific settings.
# Keep private paths, tokens, and work-only settings out of this repository.

# export EDITOR=nvim
# export SSH_SOCKS_HOST=your-ssh-config-host
# export SSH_SOCKS_PORT=16668
